---
name: dispatch
description: "Split a requirement into subtasks and route each to the best AI provider (codex/gemini/opencode/droid/mlx/ollama). Auto-checks provider health and presents a routing plan for confirmation before dispatching."
metadata:
  short-description: "Multi-provider task dispatch with auto-routing"
---

# Dispatch — Multi-Provider Task Router

Split a work requirement into subtasks and route each to the optimal AI provider based on task characteristics, provider strengths, and availability.

Triggers on: dispatch, 分派, 分配任務, 派工, route task, 拆分任務, 任務分配, multi-provider

Examples:

- `/dispatch 需要重構 API 端點並更新文件`
- `/dispatch Review the auth module, brainstorm UX improvements, and benchmark performance`

For full instructions, see `references/flow.md`
