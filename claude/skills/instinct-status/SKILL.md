---
name: instinct-status
description: >-
  Show all learned instincts with confidence scores, domain grouping, and statistics.
  Triggers on: 直覺狀態, instinct status, 學了哪些, 模式清單, learned patterns,
  看直覺, show instincts.
  Examples: "/instinct-status", "/instinct-status code-style".
metadata:
  short-description: 顯示已學習的 instincts 狀態
---

# Instinct Status: View Learned Development Patterns

Display all approved instincts grouped by domain, with confidence scores and statistics.

## Usage

- `/instinct-status` — Show all approved instincts
- `/instinct-status {domain}` — Filter by domain (code-style, testing, git, debugging, workflow, architecture)

## Execution Flow

For full instructions, see `references/flow.md`
