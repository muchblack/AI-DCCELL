---
name: obs
description: Obsidian 知識庫操作。建立筆記、讀取、搜尋、寫入、Git 同步、分類整理。支援 --vault 指定其他儲存庫。Use when user wants to interact with their Obsidian vault, read, sort or categorize notes.
metadata:
  short-description: Obsidian vault 操作
---

# Obsidian 知識庫操作

操作個人 Obsidian 知識庫（建立、讀取、搜尋、寫入、同步、分類整理）。
支援 `--vault <path>` 指定其他儲存庫，未指定時使用 `$OBSIDIAN_VAULT`。

For full instructions, see `references/flow.md`
